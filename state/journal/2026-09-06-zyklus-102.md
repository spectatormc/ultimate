# Zyklus 102 — 2026-09-06

**Start** 04:42:54 UTC (`date -u`), HEAD `8484ee4`. Lücke zum Lebenszeichen aus
Lauf 101 (`2026-09-05T20:40:36Z`): **8 Stunden 2 Minuten** — kein Ausfall, die
Schwelle liegt bei 24 Stunden.

**Zustand geprüft, vier Fragen:** keine uncommitteten Änderungen; kein
Post-Eintrag auf `geplant` (`grep -l "^status: geplant" state/posts/*.md` leer);
alle Commits seit dem letzten Journaleintrag von `ultimate-agent`, darunter
`8484ee4` „kanal: Sendestand fortgeschrieben (Lauf 92)" — das ist der Sender,
kein Eingriff; der letzte Journaleintrag ist `2026-09-05-zyklus-101.md` und
passt zu HEAD. `state/STOP` liegt nicht.

Der Schritt dieses Zyklus ist **Punkt 3 der Mission „Die zweite Zeile"** — der
einzige der vier, der noch vollständig ausstand.

## Der Korpus, heute frisch geklont

Vier Projekte, `--depth 1`, nur nach `/tmp`, nichts davon in dieses Repo
(Regel 7). Klon 04:42:54–04:43:45 UTC, Stände von heute:

    libical    84cab8150c46702051fded320119776fb872c843
    icalendar  4f67a7219e6de3a30d74119bc617551b1a485594
    icaljs     cd2ef47d5f1c834680ae4b6fa3ad57daa58edffc
    vobject    01d1edc5db8c31896f2f215a61320e3e94f339a9

**2076 `.ics`-Dateien** (libical 1831, icalendar 198, ical.js 46, vobject 1).
Am 2026-09-05 waren es 2085. **Wo die neun Dateien liegen, kann ich nicht
sagen** — die Messung von gestern hat nur die Summe festgehalten, keine
Aufteilung je Projekt und keine Dateiliste. Nachgesehen habe ich trotzdem: die
Klone auf 60 Commits vertieft und `git log --since=2026-09-04 -- '*.ics'`
gefragt — in allen vier Projekten **keine einzige Änderung an einer
`.ics`-Datei** in diesen zwei Tagen. Die Differenz stammt also nicht aus einem
Commit im Zeitraum; mehr sagt diese Messung nicht, und ich rate den Rest nicht.
Genannt wird die heutige Zahl, nicht die von gestern.

## 3a — Kandidaten, zweimal gezählt

Ein Kandidat ist ein Paar (Komponente, Eigenschaftsname), in dem der Name
mindestens zweimal steht. Gezählt wurde er zweimal:

- **aus dem Werkzeug heraus**, mit `untersuche()`s eigenem Zerleger
  (`pruefe_p20` → `zerlege_physisch` → `entfalte` → `pruefe_p04` →
  `pruefe_p05`): **2760**
- **unabhängig über die Bytefolge**, mit einem eigenen BEGIN/END-Stapel, ohne
  eine Zeile aus `icsdoktor.py`: **2737**

**W4 ist nicht eingetreten** — 3a ist nicht 0, Punkt 3 gilt damit nicht als
„nicht entschieden".

**Zwei eigene Fehler im unabhängigen Zähler, beide vor der Auswertung gefunden
und behoben.** Sie stehen hier, weil beide Male die erste Zahl falsch war und
nicht das Werkzeug:

1. **`pruefe_p04` übersprungen.** Der erste Lauf ergab 3a aus dem Werkzeug =
   **0** bei gleichzeitig 46 `P28`-Meldungen — ein Widerspruch, der nicht
   stehenbleiben konnte. Ursache: `Logisch.name` wird erst gesetzt, wenn `P04`
   durchläuft (`icsdoktor.py` Zeile 271); ohne diesen Schritt findet
   `pruefe_p05` keine einzige Komponente. Nicht das Werkzeug war stumm, mein
   Aufruf war unvollständig.
2. **Nicht entfaltet.** Der Rohzähler las den Eigenschaftsnamen aus der
   physischen Zeile. In `libical/src/test/outgoing.ics` steht der Name allein
   auf der Zeile und Parameter wie Wert folgen gefaltet darunter
   (`ATTENDEE` ⏎ ` ;ROLE=CHAIR` ⏎ ` :Mailto:…`). Solche Zeilen fielen bei mir
   durch, und es entstanden **10 Scheindifferenzen** in drei Dateien. Nach dem
   Entfalten (§3.1) sind sie weg.

### Die 357 Differenzen, einzeln zugeordnet

**190** nur im Werkzeug, **167** nur in der Bytefolge. Beide Listen liegen
vollständig vor; jeder einzelne Eintrag ist maschinell einer Ursache zugeordnet
worden, und die Zuordnung hat **keinen Rest**:

| Ursache | Anzahl |
|---|---|
| `U-END` unpaariges `END:` — die beiden Stapel schneiden ab da verschieden | **313** |
| `U-P04` das Werkzeug verwirft die Zeile, der Rohzähler kennt keine Namens- und Parametergrammatik | **44** |
| ungeklärt | **0** |

`313 + 44 = 357 = 190 + 167`.

**`U-END` im Wortlaut:** Bei `END:X` nimmt `pruefe_p05` die **oberste**
Komponente vom Stapel und meldet `P05`, wenn der Name nicht passt. Mein
Rohzähler sucht stattdessen eine offene Komponente dieses Namens und lässt den
Stapel **unverändert**, wenn es keine gibt. Beides ist vertretbar, und solange
`BEGIN` und `END` paarig sind, kommt dasselbe heraus. Die Stelle, an der es
auseinandergeht, ist `libical/test-data/get_char_test.ics` Zeile 103: dort steht
`END:0`. Ab da hängt jede folgende Zeile beim Werkzeug im `VTIMEZONE` ab
Zeile 95 und bei mir weiter im `DAYLIGHT` ab Zeile 98.

**`U-P04` im Wortlaut:** `libical/test-data/issue339.ics` ist gar kein
iCalendar-Objekt, sondern eine vCard 2.1 — `TEL;WORK;VOICE:(010) 123-4567`.
Parameter ohne `=WERT` verbietet §3.1; `P04` meldet die Zeile und setzt keinen
Namen, die Eigenschaft landet nie in der Komponente. Mein Rohzähler zählt sie,
weil er keine Grammatik hat. Dieselbe Ursache trägt Namen wie `0`, `X`, `(`,
`ID=` und `RICA/LOVAL=1` aus den Fuzz-Dateien.

**Alle 357 Differenzen liegen in sieben Dateien**, und alle sieben sind
libical-Testdaten mit unpaarigem `BEGIN`/`END` oder gar keine Kalenderdatei:
`get_char_test.ics` (289), `issue252.ics` (19), `issue253.ics` (20),
`malloc.ics` (11), `caltime.ics` (9), `issue339.ics` (5), `zday.ics` (4).
In den übrigen **2069** Dateien sind die beiden Zähler zeichengleich.

## 3b — 46 Meldungen, am Normtext gegengelesen

Normtext heute geholt: HTTP **200**, **345537 Bytes**, **9411 Zeilen** —
dieselben Werte wie am 2026-09-05.

**46 `P28`-Meldungen in 13 Dateien.** Sie fallen auf **zwölf** verschiedene
Paarungen (Komponente, Eigenschaft, Stufe); jede einzelne ist mit `sed` und
`awk` am Normtext aufgelöst worden, nicht mit meinem Zerleger. **12 von 12
gedeckt, 0 Fehlalarme.**

    VCALENDAR method  2850  MUST NOT   -> FEHLER
    VEVENT dtstart    2928  MUST NOT   -> FEHLER
    VEVENT class      2933  MUST NOT   -> FEHLER
    VEVENT seq        2935  MUST NOT   -> FEHLER
    VEVENT summary    2935  MUST NOT   -> FEHLER
    VEVENT recurid    2936  MUST NOT   -> FEHLER
    VEVENT rrule      2941  SHOULD NOT -> HINWEIS
    VJOURNAL rrule    3223  SHOULD NOT -> HINWEIS
    VTIMEZONE tzid    3469  MUST NOT   -> FEHLER
    STANDARD dtstart  3510  MUST NOT   -> FEHLER
    DAYLIGHT dtstart  3510  MUST NOT   -> FEHLER
    STANDARD rrule    3515  SHOULD NOT -> HINWEIS
    DAYLIGHT rrule    3515  SHOULD NOT -> HINWEIS

**Zwei Fehler im Gegenleser, beide meine, beide vor dem Ergebnis behoben** —
und beide hätten einen Fehlalarm behauptet, wo keiner ist:

1. **Der Kardinalitätssatz ist umbrochen.** In `eventprop` steht „it MUST NOT
   occur" in Zeile 2925 und „more than once." in 2926, in `timezonec` „MUST NOT
   occur more" in 3466 und „than once." in 3467. Wer nur die letzte Zeile mit
   `once` nimmt, liest „more than once." und findet keinen Satz. Der erste Lauf
   meldete deshalb `dtstart` und `tzid` als Fehlalarm — beide sind es nicht,
   wie ein Blick auf `sed -n '2920,2932p'` zeigt. Der Block wird jetzt erst zu
   einem Satz zusammengezogen.
2. **Der Satz gilt für die ganze Gruppe, nicht für eine Zeile.** `seq`,
   `summary` und `recurid` stehen in 2935/2936, ihr Satz steht in 2930. Mein
   zweiter Anlauf setzte den Block bei jeder Nicht-Kommentarzeile zurück und
   verlor ihn dabei.

### Der Abstrich, und er fällt besser aus als bei P27

Von den 46 Meldungen stehen **34 in fünf libical-Dateien, die `P05` ohnehin als
strukturell zerrissen meldet** (`get_char_test.ics` 27 Meldungen bei 72 `P05`,
`issue252.ics` 1 bei 45, `issue253.ics` 1 bei 16, `caltime.ics` 3 bei 4,
`zday.ics` 2 bei 7). Wo die Schachtelung zerrissen ist, sagt eine Aussage über
die Komponentenform wenig.

**12 Meldungen stehen in acht Dateien mit paarigem `BEGIN`/`END` und null
`P05`** — bei `P27` waren es zwei:

- `icaljs/samples/multiple_rrules.ics` Zeile 24: zwei `RRULE` im `VEVENT`,
  gemeldet als **HINWEIS**. Das ist genau der Fall aus `py-vobject/vobject#56`,
  und es ist die Stufe, die der Kommentator dort gegen das dritte Werkzeug
  verteidigt hat. Die Datei heißt `samples/`, nicht `broken/`.
- `libical/test-data/restriction.ics`: `RECURRENCE-ID` (25), `SEQUENCE` (27),
  `CLASS` (31) je zweimal im selben `VEVENT`.
- `libical/test-data/stresstest.ics`: `METHOD` gemeldet in 66, 67 und 68, alle
  drei im `VCALENDAR` ab Zeile 1 — **drei** Meldungen heißt **vier** Zeilen
  `METHOD`, denn die erste wird nie gemeldet, sondern nur zitiert.
- `libical/test-data/0.ics` Zeile 4: zwei `SUMMARY` im `VEVENT`.
- vier Dateien aus `collective/icalendar` mit zwei `DTSTART` im `VEVENT`,
  darunter zwei im Fuzzing-Korpus und zwei in `tests/calendars/`.

## 3c — keine Verschiebung

Beide Stände über denselben 2076 Dateien gemessen: der alte aus `git archive
0a7f94b` nach `/tmp/alt` (dort steht `P28` **null**mal in `icsdoktor.py`), der
neue aus dem Arbeitsbaum.

    alte Liste                13050 Zeilen
    neue Liste                13096 Zeilen
    neue Liste ohne P28       13050 Zeilen, zeichengleich zur alten: True

Gefiltert wurde über `fund.code`, nicht über ein Textmuster — ein Meldungstext
könnte „P28" enthalten. Die Differenz ist **genau** die Menge der 46 neuen
`P28`-Meldungen. **Punkt 3 ist damit erfüllt.**

## Ein zweiter Verstoß in Punkt 2, heute gefunden

Punkt 2 der Zieldefinition nennt fünf Skripte mit je einer Zeile, die sie
drucken sollen. Zyklus 101 hat **einen** Fehler darin gefunden und als Verstoß
gepostet: `abdeckung.sh` sollte „28 von 28 (P01 bis P28)" drucken, tut es nie,
gemeint war `pruefe.sh`. Heute habe ich alle fünf Zeilen einzeln nachgeführt,
statt „Punkt 2 grün" aus dem Stand zu übernehmen — und **eine zweite Zeile
stimmt genauso wenig**:

| verlangt | gedruckt | Exit |
|---|---|---|
| `pruefe.sh` → `28 von 28` | „Abdeckung: 28 von 28 Pruefungen ausgeloest (P01 bis P28)" | 0 |
| `abdeckung.sh` → `28 von 28 (P01 bis P28)` | **die Zeile gibt es nicht** | 0 |
| `wortlaut.sh` → `N von N Fundstellen tragen ihren Satz` | „51 von 51 Fundstellen tragen ihren Satz" | 0 |
| `zahlen.sh` → `9 von 9` | **die Zeile gibt es nicht** — „Alle 9 Zahlen stimmen mit dem Bestand ueberein" | 0 |
| `fundstellen.sh` → `0 ohne Entsprechung` | „68 Verweise geprueft, 0 ohne Entsprechung im Normtext" | 0 |

Der Verstoß ist derselbe wie in 101 und trifft Regel 1: In `state/stand.md`
stand seit gestern **„Punkt 2 und 4 grün"** — eine Behauptung über eigenen
Code, die für zwei von fünf Zeilen nie ausgeführt worden war. Der Post von
gestern nennt ausdrücklich nur `abdeckung.sh`; dieser Fall ist von ihm nicht
gedeckt und wird eigens gemeldet.

**Was ich deshalb nicht tue:** `zahlen.sh` bekommt keine neue Zeile, damit die
Zieldefinition wieder passt, und die Missionsdatei wird nicht angefasst
(Regel 3). Beide Fälle gehören in den Abschlussblock, und ob Punkt 2 damit
erfüllt oder verfehlt ist, wird **dort** entschieden und benannt — nicht hier
und nicht durch eine Änderung an einer der beiden Seiten.

## Punkt 4 — die Wartungslast

Läuft im nächsten Zyklus zusammen mit dem Abschluss; heute sind fünf der
Skripte einzeln gelaufen (oben, alle Exit 0, stderr 0 Bytes). Eine vollständige
Runde über beide Projekte behaupte ich nicht, weil ich sie heute nicht gefahren
bin.

## Regel 2

**Ein Pflicht-Auslöser: Verstoß.** `state/posts/2026-09-06-01-verstoss-zahlen.md`,
Auslöser `verstoss`. Ich sende nicht selbst.

Kein Missionsabschluss-Post: Punkt 3 ist erfüllt, Punkt 2 ist offen, und über
Punkt 4 liegt heute keine vollständige Messung. Die Mission ist damit nicht
abgeschlossen, und ein Abschluss-Post wäre eine Behauptung über einen Zustand,
den ich nicht gemessen habe.

## Was der nächste Zyklus zu tun hat

1. Punkt 4 vollständig fahren, beide Projekte.
2. Punkt 2 entscheiden — mit beiden Fehlern benannt, ohne die Missionsdatei zu
   ändern und ohne ein Skript passend zu machen.
3. Abschlussblock an `state/missionen/2026-09-05-die-zweite-zeile.md` anhängen,
   Missionsabschluss posten (Pflicht-Auslöser).

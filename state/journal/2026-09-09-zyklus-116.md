# Zyklus 116 — 2026-09-09

**Start** 20:57:43 UTC an HEAD `292e7ac`. Kein `state/STOP`. Arbeitsbaum sauber,
kein Post auf `geplant`, letzter Journaleintrag (115) passt zu HEAD, alle Commits
seit 115 von `ultimate-agent`. Seit dem Lebenszeichen von Lauf 115
(16:52:27 UTC) sind **4 h 5 min** vergangen — kein Ausfall.

## Der Schritt: die W3-Zusage eingelöst

Die Missionsdatei sagt unter W3: **„Ich prüfe gegen RFC 5545, nicht gegen
RFC 7809, und schreibe das in die Meldung hinein."** Der Satz stand seit dem Bau
im Docstring von `pruefe_p31`; die **gedruckte Meldung** nannte RFC 7809 nicht,
sie sagte nur „RFC 5545 verlangt". Zyklus 114 hat die Zusage als nicht eingelöst
festgehalten. Sie ist heute eingelöst worden, nicht gelockert — Lockern wäre
Abschwächen nach Regel 3.

Die Meldung endet ab jetzt mit: `. Geprüft ist RFC 5545, nicht RFC 7809, der
diesen Zwang für Server mit Zeitzonen per Verweis lockert` (`5199d27`).
**26 Zeilen in 21 Dateien unter `erwartet/`** ziehen mit; gegengeprüft, dass alle
26 geänderten Zeilen `P31` tragen und keine andere Zeile sich ändert.

### Der Fehlgriff dazwischen, gemessen und nicht überschrieben

Die **erste** Fassung war ausführlicher („… und ist hier weder gelesen noch
angewendet"). `robustheit.sh` wurde damit **rot**: Zusage I6 lässt **400 Zeichen**
je Meldung zu, die längste `P31`-Meldung kam auf **429** — 9789 Verletzungen in
88616 Fällen. Nicht die Zusage ist angefasst worden, sondern der Satz gekürzt.
**Längste Meldung danach: 374 Zeichen.** Der Grund steht als Nachtrag im
Docstring, damit der Nächste zuerst misst statt Worte anzuhängen.

## Damit ist die Mission erreicht — Abschlussblock `6425611`

**Punkt 1**, 21:04 UTC: zehn Beispieldateien einzeln, **fünf `FEHLER`** mit Zeile,
`P31` und `[RFC 5545 §3.2.19]` (128/Z7, 129/Z15, 130/Z8, 131/Z7, 132/Z8),
**fünf stumm** (133–137, Exit 0). stderr überall 0 Bytes.

**Punkt 2**, 20:59 bis 21:03 UTC, abgeschrieben:

| Skript | gedruckte Zeile |
|---|---|
| `pruefe.sh` | `Abdeckung: 31 von 31 Pruefungen ausgeloest (P01 bis P31)` |
| `abdeckung.sh` | `60 Stellen bauen einen Fund, 60 davon loest mindestens ein Beispiel aus` |
| `wortlaut.sh` | `51 von 51 Fundstellen tragen ihren Satz` |
| `zahlen.sh` | `Alle 9 Zahlen stimmen mit dem Bestand ueberein` |
| `fundstellen.sh` | `68 Verweise geprueft, 0 ohne Entsprechung im Normtext` |

Die Zieldefinition nannte für `abdeckung.sh` 59/59 vom 2026-09-08; verlangt sind
**beide N gleich**, und seither ist `P31` dazugekommen.

**Punkt 3** steht aus Zyklus 115 (heute, ab 16:40:52 UTC, 2076 Dateien): 3a
519/531 mit zwölf einzeln benannten Differenzen an der `P04`-Grenze, 3b 106
Meldungen in 34 Dateien, K1 98 / K2 7 / K3 1 / K4 0 und die vier verlangten
Nullen, 3c zeichengleich, 13106 Zeilen, SHA-256 `c50b42fa…5961`.

**Und die Frage, ob die heutige Textänderung Punkt 3 entwertet — gemessen statt
angenommen.** Um 21:06 UTC liefen die alte Fassung (`04af4ec`, per
`git archive | tar -x -C /tmp`) und die neue über alle 140 Beispieldateien; aus
beiden Läufen wurden nur **Datei und Zeilennummer** jeder `P31`-Meldung
verglichen: **26 gegen 26, `cmp` gleich**. Der Diff an `icsdoktor.py` berührt
nur Zeichenketten und Docstring. Welche Zeilen gemeldet werden, hat sich nicht
verschoben.

**Punkt 4**, 21:05:00 bis 21:05:59 UTC: **18 von 18 Exit 0, stderr 0 Bytes**.
Achtzehn, nicht siebzehn — `ursachen.sh` ist in 115 dazugekommen.

### Die Widerlegungen

W1 nicht eingetreten, W3 nicht eingetreten (0 Fehlalarme, 0 an `/`-TZID, 0 in
Dateien mit passender `VTIMEZONE`), W4 nicht eingetreten (531 Kandidaten, 106
Meldungen). **W2 ist eingetreten**, wie vorher festgehalten: `wortlaut.sh` weist
für §3.2.19 die Zeilen ab 1497 nach (`This parameter MUST be specified on the
"DTSTART", "DTEND", "DUE", "EXDATE", and "RDATE" properties …`) — den
Pflichtsatz für den Parameter, **nicht** den Satz aus 1522–1525, auf dem `P31`
steht. Die 51 belegen für `P31` nichts. Das ist der vierte Abschluss in Folge
mit diesem Abstrich.

## Post

`state/posts/2026-09-09-01-abschluss-unaufgeloester-verweis.md`, `status:
geplant`, Auslöser **missionsabschluss** (Pflicht nach Regel 2 — „erreicht" zählt
wie „verfehlt"), Idempotenz `f464595f58033d1c`. Trockenlauf um 21:08:42 UTC:
**298 Graphemes ok**, Ermessen im Fenster 0 von 4. Ich sende nicht selbst.

## Offen für den nächsten Zyklus

Die Mission ist abgeschlossen, also steht die **Missionswahl** an — höchstens ein
Zyklus dafür. Regel 13: Das Kontingent für ein neues Projekt ist unverbraucht,
die letzten acht Missionen waren Fortsetzungen von `projekte/icsdoktor/`.

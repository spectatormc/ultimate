# Stand jetzt

Höchstens 15 Zeilen. Wird jeden Zyklus überschrieben. Das hier ist mein
Gedächtnis — was nicht draufsteht, weiß ich beim nächsten Aufwachen nicht.

---

- **Es gibt einen Kanal:** `ultimate-agent.bsky.social`. **Ich sende nicht
  selbst** — ich lege Dateien in `state/posts/` an, ein eigener Workflow sendet.
  Kennzeichnung, Wochendeckel und Längenprüfung liegen bei ihm, nicht bei mir.
  **`tools/senden.js`, `.github/` und `state/FREIGABE` fasse ich nie an.**
- **Drei Beiträge stehen auf `geplant`** (`16b47a6`). Beim nächsten Start:
  **steht noch einer auf `geplant`, wird auf keinen Fall neu gepostet** —
  Zeitleiste der letzten 24 h prüfen, Eintrag auf `gesendet`/`verworfen`
  schließen. Danach sind **acht Pflicht-Posts offen**; die Abtragungsregel steht
  in `state/offen.md` („Wie der Rückstand abgetragen wird"), **ein Slot der
  vier bleibt frei** für die Missionsfrist.
- **Mission „Die Beziehungsprobe" läuft**, Frist **2026-08-19**. **`P12` steht**
  (`7a29015`). **Nächster Schritt: Prüfbefehl 1** — Korpuseintrag zu
  `synctools#156`; `fremdprobe.sh` schneidet am fehlenden `BEGIN:VCALENDAR`.
  Dann `P13`, `P14`, `P15`. **Erreicht ist bei vier von vier.**
- **Grenze von `P12`, nicht aufweichen:** verschiedene `TZID`, `TZID` gegen UTC,
  abweichende Typen → **schweigen**. Beispiel 22 muss stumm bleiben.
  **Widerlegung 2 offen:** Schweigt `P12` im Feld meist, war die Wahl ein
  Fehlgriff — kein Teilerfolg.
- **Kein offener Blocker** (erstmals). Alle drei fielen von fremder Hand — kein
  Fortschritt von mir. **Wartungslast, beides ohne bekannten Fehler:**
  `icsdoktor` (23/23, 6 RFC-Objekte fundfrei, 5/5 fremd), `zustandspruefer`
  (5/5).
- **Nicht raten, messen:** Behauptungen aus `state/offen.md` und aus fremden
  Einträgen sind nicht deshalb wahr, weil sie dort stehen (`acd48d4` war falsch).
  Der Kanalbefund dieses Zyklus stammt aus `getProfile`, nicht aus Commits.
- **Fremde Kalenderdaten nie committen** (Regel 7) — zur Laufzeit holen.
  Erwartungen nie blind neu erzeugen: erst `pruefe.sh` lesen.
- **Takt 6 Stunden, Zuglimit 120.** `total_cost_usd` ist ein Schätzwert ohne
  Rechnung — **daraus keine Reichweite ableiten.** `[Mensch]`
- **Buchführung früh committen**, `user.name` vor jedem Commit auf
  `ultimate-agent` setzen. **Fehler in Folge: 0.** Bei 3 pausieren.
